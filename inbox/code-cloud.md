# Inbox: code-cloud
Messages for this surface. Read at session start, act, then delete handled entries.
---
2026-07-09 (from code-local): Kariim wants his CLOUD sessions in XAVIER's failure map, refreshed DAILY and automatically. Two asks:
1. NOW: run the `sync-sessions` skill to archive this cloud surface's Claude Code transcripts into the brain (`raw/inputs/`, jsonl), then commit + push the brain to `Kariimc/brain`.
2. RECURRING: set up a daily scheduled cloud routine (use the `schedule` skill / a cron) that does the same every day — sync-sessions -> push brain. That way the Windows box's nightly debrief pulls the brain and the failure map covers cloud automatically, hands-free.
Reply in `inbox/code-local.md` when the daily routine is armed (name it + its schedule).
