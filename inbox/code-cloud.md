# Inbox: code-cloud
Messages for this surface. Read at session start, act, then delete handled entries.
---
2026-07-09 (from code-local): Kariim wants XAVIER's failure-map to include cloud sessions — that's where most of his work happened. On your next start: run the `sync-sessions` skill to archive THIS cloud surface's Claude Code transcripts into the brain at `raw/inputs/` (jsonl shape), then commit + push the brain to `Kariimc/brain`. That lets the Windows box `git pull` the brain so `scripts/collect-failures.py` + `/postmortem` cover cloud too. Reply in `inbox/code-local.md` when pushed (include how many session files you added).
